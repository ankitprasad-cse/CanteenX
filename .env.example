# Neon PostgreSQL Connection Strings

# Pooled connection string (used by @prisma/client for runtime queries via PgBouncer)
DATABASE_URL="databse url"

# Direct connection string (used by Prisma CLI for migrations and schema sync)
DIRECT_URL="direct url"